# Real zip fixtures

Samples from actual bespoke jobs. Used by `Streamline.Files.Zip.Tests`
and end-to-end tests in `Streamline.Integration.Tests`.

**Populated in Phase 3b.** Empty today.

Redact or derive synthetic equivalents if any entry contains PII.

Add one line here per fixture describing the source job, the
dispatch mode the zip exercises, and the test that references it.
