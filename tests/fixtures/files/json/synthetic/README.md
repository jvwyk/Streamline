# Synthetic JSON fixtures

Deliberately-pathological JSON files for the reader's edge cases.

**Populated in Phase 3.** Empty today.

Intended coverage:

- Object arrays
- NDJSON (newline-delimited)
- Nested-array-in-object
- Flattening of nested objects (dot-path column names)
- Missing-vs-null field distinction
- Inconsistent shapes across records

Add one line here per fixture describing the pathology and the test
that references it.
